defmodule Rez.Parser.MixinParserTest do
  # The <#mixin> element declaration syntax has been removed.
  # Mixins are now declared as $mixins: #{...} attributes in element bodies
  # or via @defaults for type-level defaults.
  use ExUnit.Case
end
